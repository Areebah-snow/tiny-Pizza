#include <stdio.h>
#include <stdlib.h>

int main(int argc, char*argv){
    int a,b;
    printf("INPUT A NUMBER");
    a=scanf();
    b=a*360;
    printf(a "multiplied by 360 is:" b);

    return 0;
    
}